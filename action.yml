name: Test Automation Reporting for GitHub Actions
description: GitHub-native test reports with failures, logs, steps, retries, screenshots, and attachments.
author: Tesults

runs:
  using: node24
  main: src/main.js
  post: src/post.js
  post-if: always()

branding:
  icon: check-circle
  color: blue
